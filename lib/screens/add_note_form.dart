import 'package:flutter/material.dart';

class AddNoteForm extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Add Note Form'),
    );
  }
}